# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=monitor_platform

# OpenAI / DeepSeek
OPENAI_API_KEY=sk-xxxx
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o

# ChromaDB
CHROMA_URL=http://localhost:8000

# Source Map storage (local directory)
SOURCEMAP_DIR=./uploads/sourcemaps

# CORS origin for the frontend dev server
CORS_ORIGIN=http://localhost:5173
