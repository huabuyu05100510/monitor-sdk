import { describe, it, expect } from 'vitest';

describe('browser entry', () => {
  it('should export the monitor', () => {
    // Basic test to ensure vitest works
    expect(true).toBe(true);
  });
});
